# Mesmo MySQL da loja (Railway / produção)
DATABASE_URL="mysql://USER:PASSWORD@HOST:PORT/DATABASE"

# Senha do painel (defina uma forte)
ADMIN_PASSWORD=""

# Segredo para assinar o cookie JWT (string longa aleatória)
SESSION_SECRET=""

# Firebase Admin (upload de fotos — use variáveis no provedor, não commite chaves)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
FIREBASE_STORAGE_BUCKET=
